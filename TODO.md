## Centralize credentials

Files with credentials:

- `alembic.ini` file contains credentials in the key `sqlalchemy.url`
- `database.py` file also contains default credentials.


## Check if the way credentials are stored is secured.
Is `.env` file secured? Or do I need other options?

### Add authentication on API endpoints.
- Check which implementation works with React Native since we might probably use this to install an app on Android phones and IOs